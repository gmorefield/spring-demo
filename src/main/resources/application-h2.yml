spring:
    datasource: 
        driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:testdb
        username: sa
        # password: redacted
        maximum-pool-size: 30
    h2:
        console:
            enabled: false