management:
    endpoints:
        access:
            default: unrestricted
        web:
            exposure:
                include: "*"

spring:
#     sql:
#         init:
#             mode: always
    output:
        ansi:
            enabled: ALWAYS